# Copy to .env.local and fill in with your Supabase project's values
# (Project Settings -> API). Leave unset to run in local-only demo mode
# (browser localStorage, no auth) with no backend required.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
